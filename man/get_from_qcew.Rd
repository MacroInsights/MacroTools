% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_from_qcew.R
\name{get_from_qcew}
\alias{get_from_qcew}
\title{Downloads employment and wage data from the BLS Quarterly Census of Employment and Wages (QCEW).}
\usage{
get_from_qcew(
  area_fips = NULL,
  cbsa = NULL,
  start_year = NULL,
  end_year = NULL,
  quarters = 1:4,
  industry_code = "10",
  ownership = 0,
  aggregate_to_msa = FALSE
)
}
\arguments{
\item{area_fips}{Character vector of 5-digit county FIPS codes
(e.g. \code{"37081"} for Guilford County, NC). Use this for any county in the US.}

\item{cbsa}{Character vector of 5-digit CBSA codes for Metropolitan Statistical Areas.
The following Piedmont Triad MSAs are built in: \code{"24660"} (Greensboro–High Point),
\code{"49180"} (Winston–Salem), \code{"15500"} (Burlington). CBSA codes are resolved to their
constituent counties internally.}

\item{start_year}{First year of data to retrieve. Default is five years before \code{end_year}.}

\item{end_year}{Last year of data to retrieve. Default is the current year.}

\item{quarters}{Integer vector of quarters to retrieve. Default is \code{1:4} (all quarters).
Pass \code{"a"} for annual aggregates.}

\item{industry_code}{NAICS industry code to filter on. Default is \code{"10"} (all industries total).
Common codes: \code{"1011"} (all private), \code{"23"} (construction), \code{"31-33"} (manufacturing),
\code{"42"} (wholesale trade), \code{"44-45"} (retail trade), \code{"62"} (health care),
\code{"72"} (leisure & hospitality).}

\item{ownership}{Ownership code to filter on. Default is \code{0} (all ownership types).
Options: \code{1} = federal government, \code{2} = state government, \code{3} = local government,
\code{5} = private.}

\item{aggregate_to_msa}{Logical. If \code{TRUE} and \code{cbsa} is provided, county-level rows are
summed to MSA level (employment and establishments summed; \code{avg_wkly_wage} is
employment-weighted). Default is \code{FALSE}.}
}
\value{
A tibble with columns: \code{area_fips}, \code{year}, \code{qtr}, \code{own_code}, \code{industry_code},
\code{qtrly_estabs}, \code{month1_emplvl}, \code{month2_emplvl}, \code{month3_emplvl}, \code{avg_monthly_emplvl},
\code{total_qtrly_wages}, \code{avg_wkly_wage}. When \code{aggregate_to_msa = TRUE}, \code{area_fips}
contains the CBSA code and an \code{area_title} column with the MSA name is added.
}
\description{
Uses the BLS QCEW public flat-file API — no API key required. Queries are made at the
county level (the finest geography the API supports) and optionally aggregated to MSA level.
}
\examples{
# All industries, Guilford County (Greensboro)
get_from_qcew(area_fips = "37081", start_year = 2020, end_year = 2023)

# Manufacturing sector, Greensboro-HP MSA aggregated
get_from_qcew(
  cbsa          = "24660",
  start_year    = 2015,
  end_year      = 2023,
  industry_code = "31-33",
  aggregate_to_msa = TRUE
)

# All three Triad MSAs, private sector, aggregated to MSA level
get_from_qcew(
  cbsa             = c("24660", "49180", "15500"),
  start_year       = 2018,
  end_year         = 2023,
  ownership        = 5,
  aggregate_to_msa = TRUE
)
}
